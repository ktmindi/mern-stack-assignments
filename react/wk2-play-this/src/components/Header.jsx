export default () => {
    return(
        <div>
            <h1> Welcome to Find Me!!</h1>
            <button>Logout</button>
            <button>My Profile</button>
        </div>
    )
}

// a different way to format components

//1) we changed the file from .js to .jsx to fix the fact that when div is typed its </div> doesnt auto populate
//2) import Header on our app.js file